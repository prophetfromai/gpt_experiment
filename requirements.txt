# This is the requirements.txt file for a Python project.
# It lists all the external libraries or packages that the project depends on.
# These packages will be installed using the Python package manager, pip.

fastapi  # FastAPI library is a modern, fast (high-performance) web framework for building APIs with Python 3.7+ based on standard Python type hints.
uvicorn  # Uvicorn is an ASGI server, used to serve FastAPI applications. It is lightweight, super-fast, and allows asynchronous programming.
firebase_admin
jinja2